---
title: Rearrangement — checking your understanding
---
import SlideLayout from '@/layouts/SlideLayout.astro';
import Callout from '@/components/Callout.astro';
import Term from '@/components/Term.astro';
import MCQ from '@/components/MCQ';

<SlideLayout title="Rearrangement — checking your understanding">

<MCQ
  client:load
  id="lim-s5-mcq-rearrange"
  prompt="The alternating harmonic series 1 − 1/2 + 1/3 − 1/4 + … converges conditionally (to ln 2). What happens if we rearrange the terms?"
  options={[
    { id: "a", label: "It must still converge to ln 2" },
    { id: "b", label: "It might converge to a different value, or even diverge" },
    { id: "c", label: "It must diverge" },
    { id: "d", label: "The sum is unchanged because addition is commutative" },
  ]}
  correct="b"
  explanation="Conditional convergence and rearrangement don't mix: the Riemann rearrangement theorem says we can permute the terms of a conditionally convergent series to converge to any value we choose — or to diverge. Addition of finitely many numbers is commutative; addition of infinitely many (as a limit) is not."
/>

<Callout kind="remark" title="Safe territory">
For nonnegative series, convergence is absolute (or the series diverges to +∞), so rearrangement is always safe. The horror story only happens with signed terms.
</Callout>

</SlideLayout>
